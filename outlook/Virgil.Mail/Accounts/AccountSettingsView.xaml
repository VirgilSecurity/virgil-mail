<UserControl x:Class="Virgil.Mail.Accounts.AccountSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="285" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Image
            Grid.Row="0"
            Margin="0 30" 
            Width="150"
            Source="/Virgil.Mail;component/Resources/wizard-logo.png" />
        <StackPanel
            Grid.Row="1"
            Margin="20 0 20 10" >
            <TextBlock 
                TextWrapping="Wrap" 
                Text="Update your account's private key settings">
            </TextBlock>
            <CheckBox 
               Margin="0 10 0 2" 
               IsChecked="{Binding IsVirgilStorage, Mode=TwoWay}" 
               Content="Do not use"
               VerticalContentAlignment="Center" />
        </StackPanel>
        <Border 
            Grid.Row="2" 
            BorderThickness="0 1 0 0" 
            Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" 
            Padding="20 20">
            <Grid>
                <Button MinWidth="90" Content="Save" Command="{Binding CreateCommand}" Padding="0 3" Background="White" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
