<UserControl x:Class="Virgil.Mail.Accounts.AccountSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="285" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Image
            Grid.Row="0"
            Margin="0 30" 
            Width="150"
            Source="/Virgil.Mail;component/Resources/wizard-logo.png" />
        <StackPanel
            Grid.Row="1"
            Margin="20 0 20 10" >
            <TextBlock 
                TextWrapping="Wrap" 
                Text="Update your account's private key settings">
            </TextBlock>
            <CheckBox 
                Margin="0 10 0 2" 
                IsChecked="{Binding IsVirgilStorage, Mode=TwoWay}" 
                Content="Save private key password"
                VerticalContentAlignment="Center" />
            <TextBlock 
                FontSize="10" 
                Foreground="Gray" 
                TextWrapping="Wrap"
                Margin="20 0 0 0" 
                Text="That option will allows you to do not enter every time password on crypto operations." />
            <CheckBox 
                Margin="0 10 0 2" 
                IsChecked="{Binding IsVirgilStorage, Mode=TwoWay}" 
                Content="Keep my private key on Virgil services"
                VerticalContentAlignment="Center" />
            <TextBlock 
                FontSize="10" 
                Foreground="Gray" 
                TextWrapping="Wrap"
                Margin="20 0 0 0" 
                Text="Your private key will be safely stored in your Virgil Security account and can easily be synchronized to another device." />
        </StackPanel>

        <Button 
            Grid.Row="1" 
            VerticalAlignment="Bottom" 
            MinWidth="90" 
            Margin="20 0 20 10"
            Content="Delete" 
            Command="{Binding RemoveCommand}" 
            Padding="0 3" 
            Background="#BB241D" 
            BorderBrush="#BB241D" 
            Foreground="White" />
        
        <Border 
            Grid.Row="2" 
            BorderThickness="0 1 0 0" 
            Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" 
            Padding="20 20">
            <StackPanel>
                <Button MinWidth="90" Content="Export Key..." Command="{Binding ExportCommand}" Padding="0 3" Background="White" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
